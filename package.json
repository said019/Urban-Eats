{
  "name": "urban-eats-loyalty",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "start": "node start.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}

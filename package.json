{
  "name": "urban-eats-loyalty",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r run start"
  },
  "engines": {
    "node": ">=18"
  }
}

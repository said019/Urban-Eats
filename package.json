{
  "name": "urban-eats-loyalty",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "start": "cd apps/api && node dist/index.js & cd apps/web && npx next start -p ${PORT:-3000}"
  },
  "engines": {
    "node": ">=18"
  }
}
